import './styles.css'

const LoadingSpinner = () => {
  return (
    <div className="loading-spinner">
      <img src="assets/logo.svg" alt="Loading..." className="spinner-logo" />
    </div>
  )
}

export default LoadingSpinner
